
// 재귀함수

/* ------------------------------------------------------------------------------------------------------------

#include <stdio.h>

void Recursive(int num) {
	if (num <= 0)							// 재귀의 탈출조건 -> 재귀함수 호출 시 탈출조건을 반드시 명시해야 한다.
		return;
	printf("Recursive call! %d \n", num);
	Recursive(num - 1);
}

int main(void) {
	Recursive(3);
	return 0;
}

------------------------------------------------------------------------------------------------------------ */
